{% extends 'base.html.twig' %}

{% block title %}MedP{% endblock %}

{% block body %}
    <h1>MedP</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ med_p.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('med_p_index') }}">back to list</a>

    <a href="{{ path('med_p_edit', {'id': med_p.id}) }}">edit</a>

    {{ include('med_p/_delete_form.html.twig') }}
{% endblock %}
