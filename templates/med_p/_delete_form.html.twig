<form method="post" action="{{ path('med_p_delete', {'id': med_p.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ med_p.id) }}">
    <button class="btn">Delete</button>
</form>
